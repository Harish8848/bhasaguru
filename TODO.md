- [x] Implement profile completion progress UI (name, phone, address) in src/components/profile.tsx
- [x] Add enrolled course details section to profile UI using enrollments from GET /api/users/[id]

- [ ] Verify profile completion calculation works in both view and edit modes
- [ ] Sanity check build/type (pnpm -s typecheck or pnpm -s build if available)
